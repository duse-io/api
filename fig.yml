db:
  image: postgres:9.4
  volumes:
    - ~/.docker-volumes/duse-api/db/:/var/lib/postgresql/data/
  ports:
    - "5432"
web:
  build: .
  command: rackup -p 5000
  volumes:
    - .:/usr/src/app
  ports:
    - "5000:5000"
  links:
    - db
